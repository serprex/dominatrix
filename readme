Experiment in code reuse for implementing a browser

JS via node's `vm` module
HTML parsing via `parse5`
CSS parsing via `css`

Node's clientside rendering is lacking, so rendering will have to be implemented on top of a C++ addon `domcore` which'll leverage `GLFW` & `FreeType2`

No widget toolkits will be used. HTML widgets will bootstrap on the dom implementation
